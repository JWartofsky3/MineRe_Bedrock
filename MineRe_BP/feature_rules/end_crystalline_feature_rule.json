{
  "format_version": "1.20.20",
  "minecraft:feature_rules": {
    "description": {
      "identifier": "minere:end_crystalline_feature_rule",
      "places_feature": "minere:end_crystalline_feature"
    },
    "conditions": {
      "placement_pass": "underground_pass",
      "minecraft:biome_filter": [
        {
          "any_of": [
            {
              "test": "has_biome_tag",
              "operator": "==",
              "value": "the_end"
            }
          ]
        }
      ]
    },
    "distribution": {
      "iterations": "t.noise = q.noise(v.originz/160,v.originx/160); return (Math.abs(v.originz) < 1000 && Math.abs(v.originx) < 1000) ? 0 : query.heightmap(v.originx, v.originz) < 10 ? 0 : (t.noise < -0.9) ? 6 : (t.noise < -0.8) ? 3 : 0;",
      "x": {
        "distribution": "uniform",
        "extent": [-16, 0]
      },
      "y": "query.heightmap(variable.worldx, variable.worldz) < 10 ? -2000 : query.heightmap(variable.worldx, variable.worldz)",
      "z": {
        "distribution": "uniform",
        "extent": [-16, 0]
      },
      "scatter_chance": {
        "numerator": 1,
        "denominator": 2
      }
    }
  }
}
